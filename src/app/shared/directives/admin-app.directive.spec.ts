import { AdminAppDirective } from './admin-app.directive';

describe('AdminAppDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminAppDirective();
    expect(directive).toBeTruthy();
  });
});
